% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dassemble.R
\name{DAssemble}
\alias{DAssemble}
\title{Differential analysis using DAssemble}
\usage{
DAssemble(
  features,
  metadata,
  core_method = NULL,
  enhancers = NULL,
  expVar,
  coVars = NULL,
  p_adj = "BH",
  orientation = c("auto", "features_by_samples", "samples_by_features"),
  contrast = NULL,
  quiet = TRUE,
  domain = c("none", "bulkrnaseq", "singlecell", "microbiome"),
  return_components = TRUE,
  return_subensembles = FALSE
)
}
\arguments{
\item{features}{Matrix or data.frame of features. Either
features x samples or samples x features (see \code{orientation}).}

\item{metadata}{Data.frame of sample-level metadata. Row names or a
column named "sample" should match sample IDs in \code{features}.}

\item{core_method}{Character: name of core method, one of
"DESeq2", "edgeR", "limmaVOOM", "dearseq",
"metagenomeSeq", "MAST", "Tweedieverse",
"Maaslin2", "Maaslin3", "LOCOM",
"LinDA", "ANCOMBC2", "Robseq", "ALDEx2",
or NULL / "none" to omit the core.}

\item{enhancers}{Character vector of up to two enhancers chosen from
"WLX", "LR", "KS". Use NULL for no enhancers.}

\item{expVar}{Character: name of primary exposure variable in \code{metadata}.
Currently assumed binary for all methods.}

\item{coVars}{Optional character vector of covariate names in \code{metadata}
(used by some core methods).}

\item{p_adj}{Multiple testing correction method passed to stats::p.adjust.}

\item{orientation}{"auto", "features_by_samples", or "samples_by_features".}

\item{contrast}{Optional contrast specification for certain core methods.}

\item{quiet}{Logical; if TRUE, core methods should be as quiet as possible.}

\item{domain}{"none","bulkrnaseq","singlecell","microbiome"; used by
enhancer normalization.}

\item{return_components}{Logical; return per-method results in $components.}

\item{return_subensembles}{Logical; if TRUE, attempt to construct CCT-based
subensembles via DA_build_subensembles (if available).}
}
\value{
A list with elements:
\describe{
\item{Method}{Label describing the ensemble, e.g. "CCT(DESeq2+WLX)".}
\item{res}{Data.frame with columns including:
\code{feature}, \code{pval_core} (if core used),
\code{pval_WLX} / \code{pval_LR} / \code{pval_KS} (if enhancers used),
\code{pval_joint} (CCT p-value), and \code{qval} (adjusted joint p-value).}
\item{features}{Original features input.}
\item{Time.min}{Elapsed time in minutes.}
\item{components}{(Optional) list of per-method result tables.}
\item{ensembles}{(Optional) sub-ensemble results if requested.}
}
}
\description{
The \code{DAssemble} function implements an ensemble strategy for differential
analysis. Users can select a single \emph{core} model and optionally augment it
with up to two \emph{enhancer} models. Enhancers are simple, often nonparametric
tests that provide complementary evidence. P-values from the core and
enhancer models are combined using the Cauchy Combination Test (CCT).
}
